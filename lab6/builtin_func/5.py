# Write a Python program with builtin function that returns True if all elements of the tuple are true.

myTuple = (False, False, True)
myTuple2 = (True, True)
print(all(myTuple))
print(all(myTuple2))

#done